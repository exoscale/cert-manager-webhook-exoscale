---
# Create a selfsigned Issuer, in order to create a root CA certificate for
# signing webhook serving certificates
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "exoscale-webhook.selfSignedIssuer" . }}
  namespace: {{ .Release.Namespace | quote }}
  {{- with .Values.annotations }}
  annotations:
{{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    app: {{ include "exoscale-webhook.name" . }}
    chart: {{ include "exoscale-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  selfSigned: {}

---

# Generate a CA Certificate used to sign certificates for the webhook
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "exoscale-webhook.rootCACertificate" . }}
  namespace: {{ .Release.Namespace | quote }}
  {{- with .Values.annotations }}
  annotations:
{{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    app: {{ include "exoscale-webhook.name" . }}
    chart: {{ include "exoscale-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  secretName: {{ include "exoscale-webhook.rootCACertificate" . }}
  duration: 43800h # 5y
  issuerRef:
    name: {{ include "exoscale-webhook.selfSignedIssuer" . }}
  commonName: "ca.exoscale-webhook.cert-manager"
  isCA: true

---

# Create an Issuer that uses the above generated CA certificate to issue certs
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "exoscale-webhook.rootCAIssuer" . }}
  namespace: {{ .Release.Namespace | quote }}
  {{- with .Values.annotations }}
  annotations:
{{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    app: {{ include "exoscale-webhook.name" . }}
    chart: {{ include "exoscale-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  ca:
    secretName: {{ include "exoscale-webhook.rootCACertificate" . }}

---

# Finally, generate a serving certificate for the webhook to use
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "exoscale-webhook.servingCertificate" . }}
  namespace: {{ .Release.Namespace | quote }}
  {{- with .Values.annotations }}
  annotations:
{{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    app: {{ include "exoscale-webhook.name" . }}
    chart: {{ include "exoscale-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  secretName: {{ include "exoscale-webhook.servingCertificate" . }}
  duration: 8760h # 1y
  issuerRef:
    name: {{ include "exoscale-webhook.rootCAIssuer" . }}
  dnsNames:
  - {{ include "exoscale-webhook.fullname" . }}
  - {{ include "exoscale-webhook.fullname" . }}.{{ .Release.Namespace }}
  - {{ include "exoscale-webhook.fullname" . }}.{{ .Release.Namespace }}.svc
